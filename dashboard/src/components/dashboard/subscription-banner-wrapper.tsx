'use client'

import { SubscriptionBanner } from './subscription-banner'

// Client wrapper for the subscription banner since it uses hooks
export function SubscriptionBannerWrapper() {
  return <SubscriptionBanner />
}
